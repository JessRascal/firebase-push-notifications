# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'firebase-push-notifications' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for firebase-push-notifications
  pod 'Firebase'
  pod 'Firebase/Messaging'

  target 'firebase-push-notificationsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firebase-push-notificationsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
